# === 🧱 БАЗОВЫЕ ЗАВИСИМОСТИ PYTHON ===
# Поддержка типов, стандартные утилиты и совместимость
annotated-types==0.7.0       # Аннотированные типы для Pydantic и других
typing_extensions==4.13.2    # Расширения typing для обратной совместимости
typing-inspection==0.4.1     # Инспекция типов (исп. в Pydantic)
packaging==25.0              # Утилита для сравнения и парсинга версий пакетов

# === 🌐 HTTP / СЕТЕВЫЕ ЗАПРОСЫ ===
httpx==0.28.1                # HTTP клиент (современный, async)
httpcore==1.0.9              # Низкоуровневая зависимость для httpx
requests==2.32.3             # Классический HTTP клиент
urllib3==2.4.0               # HTTP-библиотека, используется requests
certifi==2025.4.26           # SSL-сертификаты для безопасных соединений
idna==3.10                   # Интернационализация доменов
charset-normalizer==3.4.2    # Определение кодировки ответа

# === 🖥️ DJANGO + WSGI ===
Django==5.1.1                # Веб-фреймворк Django
gunicorn==23.0.0             # WSGI сервер для Django (продакшен)
psycopg2-binary==2.9.10      # PostgreSQL-адаптер для Django

# === 🎨 DJANGO UI + FORM HELPERS ===
django-bootstrap5==24.3      # Bootstrap 5 теги для Django
django-widget-tweaks==1.5.0  # Гибкая настройка форм в шаблонах

# === 🔄 DJANGO CELERY (ФОНОВЫЕ ЗАДАЧИ) ===
celery==5.5.2                # Основной Celery
django-celery-beat==2.8.1    # Планирование задач в Django
billiard==4.2.1              # Мультипроцессинг библиотека для Celery
kombu==5.5.3                 # Транспорт сообщений (AMQP) для Celery
amqp==5.3.1                  # AMQP-протокол
vine==5.1.0                  # Async утилиты для kombu
redis==6.2.0                 # Redis клиент

# === ⏰ ВРЕМЯ И РАСПИСАНИЕ ===
python-crontab==3.2.0        # Утилита для работы с crontab
cron-descriptor==1.4.5       # Человеко-понятное описание cron-выражений
schedule==1.2.2              # Простое планирование задач
tzdata==2025.2               # Таймзоны
pytz==2024.2                 # Поддержка таймзон
django-timezone-field==7.1   # Поле с часовым поясом для моделей

# === 🧠 МАШИННОЕ ОБУЧЕНИЕ / NLP ===
scikit-learn==1.5.2          # ML-модель и классификация
scipy==1.15.3                # Научные вычисления
joblib==1.5.1                # Сериализация моделей
nltk==3.9.1                  # Обработка естественного языка
numpy==2.2.6                 # Базовые матричные операции

# === 📊 АНАЛИТИКА / ТАБЛИЦЫ / EXCEL ===
pandas==2.2.3                # Работа с табличными данными
openpyxl==3.1.5              # Чтение/запись Excel-файлов
et_xmlfile==2.0.0            # Зависимость openpyxl

# === 🖼️ ГРАФИКА / ИЗОБРАЖЕНИЯ ===
pillow==11.2.1               # Работа с изображениями (аватары)

# === 🧪 DJANGO УТИЛИТЫ ===
django-filter==24.3          # Фильтрация данных в QuerySet
sqlparse==0.5.3              # Разбор SQL (используется в Django ORM)

# === 🔒 БЕЗОПАСНОСТЬ / ШИФРОВАНИЕ ===
rsa==4.9.1                   # RSA-шифрование (используется Telethon)
pyasn1==0.6.1                # ASN.1-парсинг (сертификаты)
pyaes==1.6.1                 # AES-шифрование

# === 💬 TELEGRAM API ===
Telethon==1.36.0             # Telegram API клиент (async)

# === 🧩 ПРОЧЕЕ ===
python-dotenv==1.0.1         # Загрузка переменных окружения из .env
prompt_toolkit==3.0.51       # Поддержка интерактивного ввода (например, в shell)
wcwidth==0.2.13              # Работа с юникод-шириной в консоли
regex==2024.11.6             # Улучшенная поддержка регулярных выражений
six==1.17.0                  # Совместимость между Python 2 и 3
click==8.2.1                 # CLI-фреймворк
click-didyoumean==0.3.1      # "Вы имели в виду..." для CLI
click-plugins==1.1.1         # Плагины для Click
click-repl==0.3.0            # REPL для Click
distro==1.9.0                # Инфо о дистрибутиве (исп. например gunicorn)
jiter==0.10.0                # Генерация данных / jinja-like шаблоны
tqdm==4.67.1                 # Прогресс-бары
openai==1.52.0               # Клиент OpenAI API
uvicorn==0.32.0              # ASGI сервер (если планируется FastAPI или WebSocket)
whitenoise==6.9.0            # Статические файлы в продакшене (Django)